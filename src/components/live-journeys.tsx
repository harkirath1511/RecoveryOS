"use client";
import { useEffect, useState } from "react";
type Journey = { id: string; orderId: string | null; state: string; outstandingAmount: number; updatedAt: string };
export function LiveJourneys() { const [journeys, setJourneys] = useState<Journey[]>([]); useEffect(() => { fetch("/api/journeys").then((r) => r.json()).then((data) => setJourneys(data.journeys ?? [])).catch(() => undefined); }, []); return <section className="panel live-panel"><div className="panel-heading"><div><p className="card-label">Live Test Mode</p><h2>Verified payment journeys</h2></div><span className="safe-pill">Neon-backed</span></div>{journeys.length === 0 ? <p className="panel-footnote">No persisted live journeys yet. Signed Razorpay events will appear here.</p> : <div className="journey-list">{journeys.map((journey) => <div className="journey-row" key={journey.id}><span>{journey.orderId ?? "Pending order"}</span><strong>{journey.state.replaceAll("_", " ")}</strong></div>)}</div>}</section>; }
