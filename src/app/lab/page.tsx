import { OperatorShell } from "@/components/operator-shell"; import { RecoveryLab } from "@/components/recovery-lab"; import { requireOperatorPage } from "@/lib/auth/operator-page";
export default async function Page(){await requireOperatorPage();return <OperatorShell title="Recovery Lab" eyebrow="Synthetic evidence"><section className="panel"><p className="source-note">Synthetic evidence only. Runs are reproducible and are never combined with verified Test Mode outcomes.</p><RecoveryLab /></section></OperatorShell>}
